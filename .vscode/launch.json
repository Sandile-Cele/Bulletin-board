{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "name": "node Server",
      "request": "launch",
      "command": "npm run start:server",
      "cwd": "${workspaceFolder}/scr"
    },
    {
      "type": "node-terminal",
      "name": "ng Serve",
      "request": "launch",
      "command": "ng serve --ssl true --ssl-cert \"Keys\\certificate.pem\" --ssl-key \"Keys\\privatekey.pem\" --open",
      "cwd": "${workspaceFolder}"
    },
    {
      "name": "Chrome",
      "request": "launch",
      "type": "pwa-chrome",
      "url": "http://localhost:4200",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
